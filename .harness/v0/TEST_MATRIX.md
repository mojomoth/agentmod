# TEST_MATRIX — per-feature test scope and completion criteria

All tests run via `go test ./...` with **no real agent installs** (temp dirs,
fixture trees, mock binaries on PATH, injected env). A feature is COMPLETE
only when its rows are all ✅ in the Status column. Update Status as tests land.

| # | Area | Must cover | Status |
|---|------|-----------|--------|
| T01 | Project discovery | found in cwd; found in ancestor; nearest-wins with nested projects; not found; stops at filesystem root | ✅ |
| T02 | Config | defaults match FABLE_PLAN §13 (change_home=false, guards on, exclusions on, XDG opt-in off); unknown schema version rejected; round-trip | ✅ |
| T03 | status | active output (roots+homes); inactive output | ✅ |
| T04 | init layout | creates .agentmod tree (claude/codex/opencode/node/snapshots); never deletes/overwrites existing dirs | ✅ |
| T05 | init idempotency | second run = no-op (full-tree snapshot); no dup .gitignore line; existing config untouched. Rc-block slice folded into T08 (rc editor doesn't exist yet) | ✅ |
| T06 | init flags | --no-shell-hook skips rc edits; non-interactive never prompts, never copies auth. Parsed/validated/reported now + tree-identical proof; rc-skip ENFORCEMENT re-tested in T08, auth-never-copy in Phase 3 (neither exists yet) | ✅ |
| T07 | .gitignore | created if missing; entry deduped; non-git dir handled gracefully | ✅ |
| T08 | rc fencing | block inserted once; no dup block on re-init (T05 slice); updated in place; user content byte-preserved around it | ✅ also: --no-shell-hook enforcement (T06 slice), newline-glue append, bash rc pick (.bashrc > .bash_profile > create .bashrc), ZDOTDIR, SHELL/HOME-unset + unsupported-shell skips, corrupt-fence hard error w/o writes, zsh -n + bash -n block syntax gate |
| T08a | init first-session diagnosis | hook-not-live caveat printed (new terminal / exec $SHELL / one-shot eval); "already loaded" hedge only when the block predates this run; AGENTMOD_ACTIVE=1 + same root → "already routing", different root → "switches at next prompt"; skips print no notice | ✅ TestInitHookActivationNotice (6-case table, fakeEnv only) |
| T09a | env transitions | `agentmod env`: activate emits routed vars + saves pre-existing values; deactivate restores/unsets (perfect-inverse round-trip); switch undoes old project before new; PATH prepend deduped + stripped; disabled agents/XDG opt-in respected; quoting safe (real bash+zsh eval smoke); failures keep stdout empty; AGENTMOD_VARS names sanitized | ✅ |
| T09 | zsh hook | scripted zsh: cd in → vars set; cd out → vars unset; new shell inside project activates (precmd); nested project nearest-wins | ✅ also: missing-binary warn-once, broken-config error-once + old-project deactivate, double-eval single registration, zsh -n syntax gate |
| T10 | bash hook | same via PROMPT_COMMAND | ✅ real /bin/bash (3.2-clean): cd in/out, nested nearest-wins, interactive PROMPT_COMMAND new-shell activation, missing-binary warn-once, broken-config error-once + old-project deactivate, double-eval appends once + keeps user PROMPT_COMMAND, bash -n syntax gate |
| T11 | env hygiene | no duplicate PATH entries after repeated transitions; prior user env values restored; HOME never changed; no shims created anywhere | ✅ integration_test.go, one scripted session per shell (zsh + bash): full `env \| sort` diff before/after (perfect inverse incl. PATH + HOME, no AGENTMOD_* leak), sentinel values (spaces/quote/$) on all routed vars + XDG_CONFIG_HOME survive in→out→in→A→B→A→out, exactly one node/bin PATH entry inside (current project's), SAVED var = user original even right after A→B switch, file-tree snapshots of both projects + fake bin dir + start dir unchanged (no shims) |
| T12 | Claude routing | CLAUDE_CONFIG_DIR → .agentmod/claude inside; unset/restored outside | ✅ env (T09a) + real-zsh hook (T09); scenario re-check in T30 |
| T13 | Codex routing | CODEX_HOME → .agentmod/codex inside; unset/restored outside | ✅ env (T09a) + real-zsh hook (T09); scenario re-check in T30 |
| T14 | OpenCode routing | partial: OPENCODE_CONFIG set, XDG untouched; opt-in: XDG_CONFIG_HOME/XDG_DATA_HOME set; off by default | ✅ partial mode env+zsh-hook (XDG1:unset asserted); XDG opt-in env-level (T09a); scenario re-check in T30 |
| T15 | Auth bootstrap | consent→copy (codex auth.json; claude .credentials.json linux path); decline→instructions; non-interactive→never copies; copied files in exclusion list | ✅ (auth_test.go: consent-copies-both incl. 0600 + content, mixed consent w/ shared-reader + EOF-partial-line, decline, nil-stdin declines, --yes/--non-interactive never prompt never copy ×2, no-global-auth no-prompt, HOME-unset, darwin Claude = Keychain note + Codex still copies, already-present re-init no-prompt + tree byte-identical, global-non-regular refuses. Exclusion-list assertion itself = Phase 5 T20; D028 records the exact paths the engine must exclude) |
| T16 | guard claude-bash | blocks rm/cp/mv/mkdir/clone/redirect into global homes; allows reads; allows project writes; unparseable input fail-safe (denies only global-path writes); exit-2 + deny-JSON modes | ✅ (guard_test.go ×2: engine table 37 commands incl. sudo/HOME=/multiline/near-miss negatives, custom-HOME literal, non-Bash tool, unparseable ×5; CLI exit-2/JSON-deny shape/allow-silent both modes/injected-HOME/nil+empty stdin/read-error fail-safe/usage errors) |
| T17 | guard wiring | init writes PreToolUse hook into .agentmod/claude/settings.json, NOT project .claude/ | ✅ (claudesettings_test.go: fresh write w/ matcher Bash + type command + quoted abs binary path, user-keys/PostToolUse/other-PreToolUse preserved on merge, present-and-correct = zero rewrite byte-identical, stale binary path repaired in place no dup entry, invalid-JSON/non-object/wrong-typed hooks ×4 hard error zero writes, whitespace-only file = empty object, project .claude/settings.json byte-identical, unresolvable binary ×2 skip w/o file, settings.json-as-dir error; re-init no-op also locked by TestInitSecondRunIsNoOp snapshot) |
| T18 | gstack | clones only into .agentmod/claude/skills/gstack; fails outside project; already-installed abort + --force; global home unchanged before/after (verified); no-git/network/setup errors distinct | ✅ (install_test.go: fixture-repo clone happy path incl. atomic no-temp-leftovers + .git kept, outside-project exit 2, already-installed abort + sentinel untouched + --force hint, clone-failure cleanup, arg validation ×4 creates nothing incl. `--force extra` rejected, --force replaces existing (old sentinel gone, new content in, no leftovers), --force-with-nothing-installed = plain install (no "Replacing" line), failed --force clone leaves old install byte-intact, global-check unchanged-line with pre-existing global entries (D010 negative), end-to-end delta VIOLATION via symlinked fake-global (exit 1, success paragraph suppressed), HOME-unset + ENOTDIR skip notes, removed-entry branch direct, diffListings table ×7, git-missing distinct message via crippled real PATH + nothing created, clone failure forwards git's own `fatal: … does not exist` words + AGENTMOD_GSTACK_SOURCE hint line, setup failure via FILE-at-claude/skills ENOTDIR fault injection — PathError passthrough + blocker byte-untouched, D033) |
| T19 | handoff create | .amod contains manifest/inventory/checksums/redaction-report/HANDOFF/RESTORE; payload matches inventory | ✅ (handoff_test.go ×2 pkgs: members present incl. empty dir, snapshots/ never packed (+ self-pack negative), manifest fields, inventory↔payload size/sha/mode/symlink-target match + sorted + count equality, exec bit in zip mode, checksums.txt covers all content members & shasum-verified in smoke, byte-identical determinism, refuse-existing-output, missing-.agentmod error, unreadable-file leaves no partial, fifo refused, default timestamped name w/ fixed clock + collision refusal, --output, outside-project exit 2, arg-validation ×8 create nothing, nil-Now fallback. REDACTION.md member landed with T21; HANDOFF/RESTORE members landed with D037 — docs_test.go asserts member presence + §12 content anchors (project/timestamp/version/platform, restore-not-implemented note, exclusion+scan summary w/ singular/plural/empty states, verbatim re-login remedies, gstack --force + node/bin reinstall notes); member-set + checksums + determinism tests cover both members) |
| T20 | exclusions | source code, .git, node_modules, caches, tmp, auth (incl. consent-copied), .env, ssh/cloud creds all absent by default | ✅ (exclude_test.go: 38-case rule table incl. kept-content negatives (sessions/logs/configs stay, tokenizer-style fuzz avoided), hostile-fixture create proves every category absent from the zip while sibling SKILL.md/sessions/logs survive, Result.Excluded path↔rule map asserted exactly incl. structural snapshots-output entry, pruned dir recorded once + payload count, empty-non-nil-Rules escape hatch packs auth + records only structural, determinism with exclusions; cli handoff_test.go: stdout names each excluded entry with its rule ID, auth absent from zip while settings.json kept, default-output structural line. Source code = structurally absent (payload is .agentmod-only); project-level payload roots get their own rule when they land, D035) |
| T21 | secret scan | fixture secrets detected → listed in redaction report; clean fixture → empty report | ✅ (scan_test.go: 16-case pattern table incl. prose/short-value negatives + line numbers, one-finding-per-pattern w/ pattern-order pinning, hostile-fixture kept files scan clean + REDACTION.md renders exclusion reasons verbatim + "No secret candidates" + never reproduces excluded content, warn finding listed in report/Result w/ matched value absent + file still packed, hard private-key refusal names path/line/pattern + --allow-findings + leaves no output, --allow-findings packs + HARD-marked in report + byte-identical determinism, excluded-file private key produces zero findings (filter-before-scan); cli handoff_test.go: refusal exit 1 → --allow-findings exit 0 round-trip w/ stdout HARD mark, warn finding on stdout w/ value absent, "secret scan: clean" line in default-output test; checksums test auto-covers REDACTION.md; shasum -c verified in binary smoke) |
| T22 | git metadata | repo/branch/HEAD/dirty/summary recorded; token in remote URL redacted; dirty tree warns, needs explicit consent | ✅ |
| T23 | inspect/verify/list | inspect shows manifest+inventory; verify detects checksum tamper + schema mismatch; list finds snapshots | ✅ (read_test.go: Open reads manifest/inventory/redaction + member/dir counts, missing-required-member error names it, not-a-zip error; Verify clean = zero problems + exact checked count, tampered payload → checksum + inventory-sha mismatch, dropped member → missing-from-archive + inventory-orphan, smuggled extra member → unlisted + not-in-inventory, schema 99 w/ regenerated checksums → exactly the newer-schema problem, malformed checksums line flagged; rewriteSnapshot tamper helper reusable for T24 malicious fixtures. cli handoff_test.go: inspect prints manifest/git-state/counts/REDACTION.md verbatim without extracting + git branch/clean/redacted-remote + missing-file exit 1; verify OK exit 0 anywhere (no project needed), tampered member + garbage file exit 3 w/ FAILED problems, typo'd path exit 1 not 3; list empty-message / newest-first ordering via Chtimes / outside-project exit 2; pack alias creates + unpack stub; arg-validation rows for inspect/verify/list. Binary smoke: full create→list→inspect→verify→pack→garbage-verify-exit-3 flow in /tmp) |
| T24 | restore safety | zip-slip entry rejected; absolute path rejected; symlink escape rejected; writes confined to .agentmod/; no script auto-exec | ✅ (validate_test.go: PlanRestore refuses zip-slip names ×3, absolute/drive/backslash/non-canonical/outside-whitelist names, root-member smuggle, §21 protected elements ×4 (.git/.ssh/.aws/.docker), hostile symlink targets ×6 via fixture-link mutation (absolute/escape/empty/backslash/drive/oversized), duplicate payload path, fifo member, newer-schema hard refusal; setuid stripped not refused; confined link chain allowed; clean fixture yields exact Dirs/Files/Links plan. Extraction half (restore_test.go, D043): fresh-root restore writes ONLY .agentmod/ (target root gains exactly one entry), O_EXCL never overwrites (duplicate-plan-entry rollback), extraction executes nothing — members are only written; cli refusal tests prove garbage/tampered snapshots exit 3 with zero disk movement) |
| T25 | restore backup | existing .agentmod backed up before write; backup restorable | ✅ (backup_test.go: rename moves the tree intact — file bytes, exec bit, symlink target — frees the .agentmod name, and rename-back is a complete rollback; absent .agentmod → no-op empty path, project root gains nothing; occupied backup name → refusal naming it, source AND occupant untouched; stray regular FILE at .agentmod backed up as-is; stamp renders UTC under a non-UTC clock zone. Before-write half (restore_test.go + cli, D043): Restore backs up ahead of the first write — backup carries the old tree + the in-tree snapshot file, deterministic name asserted from the cli with the fixed clock; failed extraction rolls back to a byte-identical tree (digest compare) leaving no backup entries; same-clock second restore refused on the occupied backup name) |
| T26 | round-trip | create→restore on fresh tree reproduces config/skills/plugins; excluded items absent + re-login notice present | ✅ (restore_test.go round trip: contents, 0600/0700/0755 modes, symlink + its resolution, empty dirs, snapshots/ recreated empty; cli round trip: marker file travels A→B, backup holds B's old config, .gitignore gains backup pattern, re-login block with canonical D037 remedies, inline doctor whose exit never propagates; darwin Keychain line gated on env.GOOS; unpack alias round trip. T30's scenario matrix re-exercises the round trip end-to-end later) |
| T27 | portability | windows-style paths normalized; exec bits restored; MCP absolute paths warned/rewritten | ✅ (separator half: windows-style names are REFUSED not normalized — D041 settled that; validate_test.go covers backslash/drive/UNC-shaped names, FromSlash joins at extraction. Exec-bit half: restore_test.go round trip proves 0755/0700/0600 survive umask-proof, D043. MCP half (D044): portability_test.go — classify table ×13 (relative/flag silent, existing abs silent, missing abs warn, inside-this-.agentmod exists/missing, foreign-.agentmod w/ local-equivalent hint, drive/UNC always-warn, 7:\ negative), localAgentmodEquivalent unit, scanner over all four config files (JSON values + arrays + TOML tables warned, guard-marker command exempt, local-resolving path silent, dedup, unparseable file → file-level warn, sorted order), absent-files-scan-clean; cli handoff_test.go: restore rewrites the guard hook command from the source machine's binary to this machine's (settings.json asserted before/after) while codex/config.toml stays byte-identical (warn, not rewrite) + warning line on stdout + exit stays 0; round-trip test asserts the fresh-wire + clean-portability lines. Binary smoke in /tmp: foreign settings.json rewritten, 2 codex warnings incl. local-equivalent hint, doctor 'wired with the current binary') |
| T28 | git handoff | --for-git output under .agentmod-handoff/; no sessions/logs/secrets/source; --include-sessions fails citing encryption; HANDOFF doc present | ✅ (D047 slice 1: gitpack_test.go — tree members + payload spot checks (exec bit, symlink-as-symlink, structural snapshots/ exclusion, empty dir), every checksums.txt line re-hashed against the on-disk tree, inventory↔tree presence, for_git marker only in tree manifests, deterministic re-create replaces a stale previous package, foreign-dir + non-dir refusals leave user content intact, hard-finding refusal keeps the previous package byte-intact / fresh run leaves nothing, git-mode HANDOFF/RESTORE doc anchors; cli: --for-git happy path (no .amod side-product) + --output/--allow-findings conflicts create nothing. D048 slice 2: ForGitRules table (anchored session/log targets + lookalikes kept + defaults still win), end-to-end sessions/logs exclusion w/ exact rule IDs + REDACTION/HANDOFF anchors, .amod-still-packs-sessions pin, cli --include-sessions refusals ×2 (encryption explanation / only-meaningful-with---for-git) creating nothing. D049: TestPackForGitAlias + TestPackForGitAliasIncludeSessionsRefused pin the `pack --for-git` alias end-to-end (manifest for_git, no .amod side-product, encryption refusal through the alias creating nothing); tree-package reader decided out of scope — Phase 7 complete) |
| T29 | doctor | each §23 warning has a positive + negative test (warn fires / stays silent) | ✅ (doctor_test.go: hook-installed-but-inactive, in-project-vars-unset, other-root stale, misrouted vars, layout missing/not-a-dir, broken config, stale/corrupt rc block, $SHELL-unset in/out, fresh-machine-clean exit 0, lingering-vars out-of-project + user-own-var negative, dup-PATH, node/bin-missing-while-active + inactive negative, foreign-.agentmod PATH entry, HOME saved/inside-.agentmod + healthy negative, shim script/escaping-symlink + npm-local-install negative, agent-binaries stat-walk found/non-executable/absent, Claude/Codex auth present / missing-is-ok-with-relogin-instructions / not-regular-file warn, disabled-agents routing-disabled, opencode.json missing warn / not-regular error, OpenCode sessions global-data-dir warn / XDG_DATA_HOME respected / no-data ok-with-limitation / XDG-opt-in ok / disabled skipped, merge-chain leaking-keys warn / trivial-config ok ×3 / unparseable warn / XDG-opt-in ok / disabled skipped, Keychain note darwin-ok / linux+unset-GOOS absent / claude-disabled absent / outside-project absent, gstack-global dir+file+symlink warn / absent ok / HOME-unset ok, gstack-project not-installed+installed ok / not-a-dir warn / outside-project absent, Claude-guard wired ok / settings-missing warn / hook-missing-from-file warn / stale-binary-path warn naming both commands / invalid-JSON error / unresolvable-Executable ×2 ok-with-caveat / outside-project absent; Agent-config-paths foreign-.agentmod + missing-abs warn ×2 in sorted order w/ local-equivalent hint / unparseable-file file-level warn while OpenCode-config stays ok (stat vs content split) / healthy ok line / outside-project absent (D046, re-surfaces the D044 scan); Snapshots secret-candidates warn from REDACTION.md counts w/ HARD marker + no-matched-bytes assertion / clean-snapshot ok / garbage-.amod warn / none-yet ok; Git-handoff hand-injected session+log leak warn w/ exact sorted rule-tagged list / clean-package ok / manifest-less-dir warn / absent ok; Git-state HEAD matches-newest-snapshot ok / post-commit drift warn (real git, skip-gated) / no-recorded-HEAD ok / no-snapshots no-line; all three absent outside a project (D053)) |
| T30 | scenarios §27 | proj00 global untouched; proj01 isolated (global plugin invisible); proj01 gstack local-only; proj02 unaffected; A→B handoff; git handoff | ✅ (scenario_test.go TestScenarioIsolationMatrix covers §27.1–.4 in real {zsh,bash} sessions with mock claude/codex/opencode binaries: superpowers visible in proj00/proj02 only, gstack in proj01 only, init runs non-interactively with auth guidance instead of stalling, install's global-skills check runs in-session against the fake HOME, proj00/proj02/global-claude trees byte-identical before/after, XDG untouched; mutation-verified (D050). §27.5: TestScenarioHandoffRoundTrip — machine-A env (CLAUDE.md, gstack skill, session transcript, MCP server w/ relative command, opencode.json + both sk-FAKE auth files) packed with exactly the two auth-file exclusions named, restored on machine B: every continuation file byte-equal, auth absent, canonical re-login block + clean-portability line, B's root gains exactly .agentmod+backup. §27.6: TestScenarioGitHandoff — source at root + .env + 2 auth + session + log fixture, one --for-git run, four exclusions asserted by path+rule-ID, manifest for_git, payload file set pinned exactly by DeepEqual (any category leak fails). Mechanics overlap mapped, not duplicated: backup/doctor → restore tests, tree format → gitpack_test.go, pack alias → TestPackForGitAlias, sessions-stay-in-.amod → TestCreateStillPacksSessionsAndLogs (D051)) |

Legend: ⬜ not started · 🟡 partial · ✅ complete (tests passing)
